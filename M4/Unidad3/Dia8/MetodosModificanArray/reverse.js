/* reverse() => El método reverse cambia el orden de los elementos del array de atras hacia adelante */

const numerosOrdenados = [1, 2, 3, 4, 5, 6];

numerosOrdenados.reverse();

console.log(numerosOrdenados);
